// Règles temporaires Firestore pour TeamUp (DÉVELOPPEMENT UNIQUEMENT)
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORAIRE : Permettre toutes les lectures/écritures pour les tests
    // ⚠️ À SECURISER EN PRODUCTION ⚠️
    match /{document=**} {
      allow read, write: if true;
    }
    
    // Règles spécifiques pour les utilisateurs (à utiliser plus tard)
    // match /users/{userId} {
    //   allow read, write: if request.auth != null && request.auth.uid == userId;
    // }
    
    // Règles pour les événements (à utiliser plus tard)  
    // match /events/{eventId} {
    //   allow read: if true;
    //   allow write: if request.auth != null;
    // }
  }
}